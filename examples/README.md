# Examples

Each example is self-contained in one directory. Its README identifies the
input file, exact command, generated files, and scientific definitions.

| Directory | Purpose |
| --- | --- |
| `afm_frame/` | Inspect a small U-N antiferromagnetic frame sequence |
| `moment_time_evolution/` | Export and plot net and local moment evolution |
